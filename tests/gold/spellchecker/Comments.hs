module Comments where

-- Should find <TYPO>mispellings</TYPO> in comments

-- | And doc <TYPO>coments</TYPO>

-- ^ And and these <TYPO>commens</TYPO> two

-- Also <TYPO>the'se</TYPO>

-- But doesn't catch this.
